/* 登录提示弹窗 - 优化版 */
.login-prompt-modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 9999;
  animation: modalFadeIn 0.3s ease-out;
}

@keyframes modalFadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

.modal-mask {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.6);
  backdrop-filter: blur(10rpx);
}

.modal-content {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) scale(0.95);
  animation: contentScaleIn 0.3s ease-out forwards;
  width: 600rpx;
  background: linear-gradient(135deg, #ffffff 0%, #FFF9ED 100%);
  border-radius: 24rpx;
  overflow: hidden;
  box-shadow: 0 20rpx 60rpx rgba(0, 0, 0, 0.2), 0 0 0 1rpx rgba(255, 255, 255, 0.1) inset;
}

@keyframes contentScaleIn {
  to {
    transform: translate(-50%, -50%) scale(1);
  }
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 36rpx 40rpx 28rpx;
  background: linear-gradient(135deg, #E85D4A 0%, #F08A4B 100%);
}

.modal-title {
  font-size: 34rpx;
  font-weight: 600;
  color: #ffffff;
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.1);
}

.modal-close {
  font-size: 38rpx;
  color: rgba(255, 255, 255, 0.8);
  padding: 12rpx;
  border-radius: 50%;
  transition: all 0.2s ease;
}

.modal-close:active {
  background: rgba(255, 255, 255, 0.2);
  transform: scale(0.9);
}

.modal-body {
  padding: 48rpx 40rpx 36rpx;
  text-align: center;
}

.prompt-icon {
  display: block;
  width: 100rpx;
  height: 100rpx;
  margin: 0 auto 24rpx;
  border-radius: 50%;
  box-shadow: 0 8rpx 20rpx rgba(255, 154, 158, 0.3);
}

.prompt-text {
  display: block;
  font-size: 32rpx;
  color: #3F302B;
  margin-bottom: 16rpx;
  font-weight: 600;
  line-height: 1.4;
}

.prompt-desc {
  display: block;
  font-size: 26rpx;
  color: #8C7770;
  line-height: 1.5;
}

.modal-footer {
  display: flex;
  gap: 20rpx;
  padding: 0 40rpx 40rpx;
}

.btn-cancel,
.btn-confirm {
  flex: 1;
  height: 92rpx;
  border: none;
  border-radius: 16rpx;
  font-size: 30rpx;
  font-weight: 500;
  transition: all 0.2s ease;
  position: relative;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
  line-height: 1;
}

.btn-cancel {
  background: #FFF9ED;
  color: #8C7770;
  border: 2rpx solid #E8DED8;
}

.btn-cancel:active {
  background: #FFF1D6;
  transform: translateY(2rpx);
}

.btn-cancel::after {
  border: none;
}

.btn-confirm {
  background: linear-gradient(135deg, #E85D4A 0%, #D94A3A 100%);
  color: #ffffff;
  box-shadow: 0 4rpx 12rpx rgba(232, 93, 74, 0.3);
}

.btn-confirm:active {
  transform: translateY(2rpx);
  box-shadow: 0 2rpx 6rpx rgba(232, 93, 74, 0.25);
}

.btn-confirm::after {
  border: none;
}

/* 按钮点击效果 */
.btn-cancel:active::before,
.btn-confirm:active::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 100%;
  height: 100%;
  background: rgba(255, 255, 255, 0.2);
  border-radius: 16rpx;
  transform: translate(-50%, -50%) scale(0);
  animation: buttonRipple 0.6s ease-out;
}

@keyframes buttonRipple {
  to {
    transform: translate(-50%, -50%) scale(4);
    opacity: 0;
  }
}
